spring:
  application:
    name: eureka-server
  devtools:
    restart:
      enabled: false
    remote:
      enabled: false
server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
